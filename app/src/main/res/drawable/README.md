The `ic_*_white.xml` vectors here are copied unmodified from
[`lightphone/light-sdk`](https://github.com/lightphone/light-sdk) (`sdk/ui/src/main/res/drawable`),
MIT licensed, so the app's chrome matches LightOS instead of approximating it.

`ic_launcher_background.xml` and `ic_launcher_foreground.xml` are generated by
`scripts/generate_icon.py` — edit the geometry there, not here.
