<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A black window background rather than the platform default: Compose paints after the
      window is up, so any other colour shows as a flash on every launch.
    -->
    <style name="Theme.LightRemote" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
